repos:
  - repo: local
    hooks:
      - id: catalog-sanitization
        name: catalog-sanitization
        entry: npm run check:catalog-sanitization
        language: system
        pass_filenames: false
